- if current_user.following?(user)
  = link_to 'フォロー解除', relationship_path(current_user.active_relationships.find_by(followed_id: user.id)), method: :delete
- else
  = button_to 'フォローする', relationships_path, {params: {followed_id: user.id} }
